name: bootstrap5
template:
  engine: pebble
modules:
  active:
    - pebble-module
media:
  formats:
    - name: small
      width: 256
      height: 256
      format: webp
      compression: true
    - name: big
      width: 512
      height: 512
      format: webp
      compression: true